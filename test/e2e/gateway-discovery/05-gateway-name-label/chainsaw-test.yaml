apiVersion: chainsaw.kyverno.io/v1alpha1
kind: Test
metadata:
  name: gw-name-label-fallback
spec:
  description: >
    When a pod has app.kubernetes.io/name label but no stoker.io/gateway-name
    annotation, the label value is used as the gateway name.
  steps:
    - name: deploy git server and api-key secret
      try:
        - apply:
            file: ../../fixtures/git-server.yaml
        - apply:
            file: ../../fixtures/api-key-secret.yaml
    - name: wait for git server ready
      try:
        - script:
            env:
              - name: NAMESPACE
                value: ($namespace)
            content: |
              kubectl wait --for=condition=Ready pod/test-git-server -n $NAMESPACE --timeout=60s
    - name: create GatewaySync CR
      try:
        - apply:
            resource:
              apiVersion: stoker.io/v1alpha1
              kind: GatewaySync
              metadata:
                name: test-gnl
              spec:
                git:
                  repo: (join('', ['git://test-git-server.', $namespace, '.svc.cluster.local:9418/test-repo.git']))
                  ref: main
                sync:
                  profiles:
                    default:
                      mappings:
                        - source: "config"
                          destination: "config"
                          type: dir
                gateway:
                  apiKeySecretRef:
                    name: ignition-api-key
                    key: apiKey
    - name: wait for ref resolved
      try:
        - script:
            env:
              - name: NAMESPACE
                value: ($namespace)
            timeout: 60s
            content: |
              for i in $(seq 1 30); do
                STATUS=$(kubectl get gatewaysync test-gnl -n $NAMESPACE -o jsonpath='{.status.conditions[?(@.type=="RefResolved")].status}' 2>/dev/null)
                [ "$STATUS" = "True" ] && exit 0
                sleep 2
              done
              echo "Timed out waiting for RefResolved=True, got: $STATUS"
              exit 1
    - name: create pod with label only
      try:
        - apply:
            resource:
              apiVersion: v1
              kind: Pod
              metadata:
                name: gateway-gnl
                annotations:
                  stoker.io/cr-name: test-gnl
                labels:
                  app.kubernetes.io/name: label-gw
              spec:
                containers:
                  - name: pause
                    image: registry.k8s.io/pause:3.9
    - name: wait for pod running
      try:
        - script:
            env:
              - name: NAMESPACE
                value: ($namespace)
            content: |
              kubectl wait --for=condition=Ready pod/gateway-gnl -n $NAMESPACE --timeout=60s
    - name: assert gateway name from label
      try:
        - script:
            env:
              - name: NAMESPACE
                value: ($namespace)
            timeout: 60s
            content: |
              for i in $(seq 1 30); do
                NAME=$(kubectl get gatewaysync test-gnl -n $NAMESPACE -o jsonpath='{.status.discoveredGateways[0].name}' 2>/dev/null)
                POD=$(kubectl get gatewaysync test-gnl -n $NAMESPACE -o jsonpath='{.status.discoveredGateways[0].podName}' 2>/dev/null)
                [ "$NAME" = "label-gw" ] && [ "$POD" = "gateway-gnl" ] && break
                sleep 2
              done
              [ "$NAME" = "label-gw" ] || { echo "Expected name=label-gw, got: $NAME"; exit 1; }
              [ "$POD" = "gateway-gnl" ] || { echo "Expected podName=gateway-gnl, got: $POD"; exit 1; }
              echo "Gateway name correctly resolved from label: $NAME (pod: $POD)"
