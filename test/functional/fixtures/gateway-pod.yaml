apiVersion: v1
kind: Pod
metadata:
  name: gateway-test-SUFFIX
  labels:
    app: gateway-test
    app.kubernetes.io/name: GATEWAY_NAME
  annotations:
    stoker.io/cr-name: "CR_NAME"
    stoker.io/gateway-name: "GATEWAY_NAME"
spec:
  containers:
    - name: pause
      image: registry.k8s.io/pause:3.9
      resources:
        requests:
          cpu: 10m
          memory: 16Mi
        limits:
          cpu: 10m
          memory: 16Mi
