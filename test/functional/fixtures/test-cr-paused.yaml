apiVersion: sync.ignition.io/v1alpha1
kind: IgnitionSync
metadata:
  name: test-sync-paused
spec:
  paused: true
  git:
    repo: "${GIT_REPO_URL}"
    ref: "main"
  gateway:
    apiKeySecretRef:
      name: ignition-api-key
      key: apiKey
