apiVersion: stoker.io/v1alpha1
kind: Stoker
metadata:
  name: test-sync-paused
spec:
  paused: true
  git:
    repo: "${GIT_REPO_URL}"
    ref: "main"
    auth:
      token:
        secretRef:
          name: git-token-secret
          key: token
  gateway:
    apiKeySecretRef:
      name: ignition-api-key
      key: apiKey
